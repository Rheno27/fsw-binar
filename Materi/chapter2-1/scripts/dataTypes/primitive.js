// String
let address = "Jember";
console.log(address);

// Number
let score = 0.1 + 0.2;
const isScore = 0.3 == score;
console.log(score); // 0.30000000000000004
console.log(isScore); // false

// Boolean
let isRainy = true; // true or false
console.log(isRainy);

// Null
let nullable = null;
console.log(nullable);

// Undefined
let UndifinedVar = undefined;
console.log(UndifinedVar);

/* 
    Notes:
        null, undefined is same, null for variable that null or empty
        undefined is variable that has not defined
    and  it is same to boolean false if we use condition
*/